Working Copy Types
====================

Kart supports the following working copy types.
The file-system working copy is used for file-formats (currently only point cloud tiles),
and any of the other working copy types can be used tabular data (storing spatial and non-spatial database tables).

.. toctree::
   :maxdepth: 2

   wc_types/filesystem_wc
   wc_types/gpkg_wc
   wc_types/mysql_wc
   wc_types/postgis_wc
   wc_types/sql_server_wc
