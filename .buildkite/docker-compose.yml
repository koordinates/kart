---
version: '3.6'

services:
  tests:
    build:
      dockerfile: ../Dockerfile
      context: ..
    command: /src/.buildkite/run-tests.sh
    user: root
    working_dir: /src
    volumes:
      - "..:/src:delegated"
    tmpfs:
      - /tmp
